AOS.init({
    duration: 1000, // Durasi animasi dalam milidetik
    offset: 200,    // Jarak offset sebelum animasi dipicu
    once: true,     // Animasi terjadi hanya sekali saat elemen muncul di layar
  });